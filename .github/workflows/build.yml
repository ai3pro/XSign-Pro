name: Build XSign-Pro (Final Fix)

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 1 # Chỉ tải bản mới nhất để nhanh hơn

      - name: Select Xcode Version
        run: sudo xcode-select -s /Applications/Xcode_16.2.app/Contents/Developer

      - name: Clean and Resolve Packages
        run: |
          xcodebuild -resolvePackageDependencies -project Feather.xcodeproj -scheme Feather -skipPackagePluginValidation

      - name: Build Archive
        run: |
          xcodebuild archive \
            -project Feather.xcodeproj \
            -scheme Feather \
            -configuration Release \
            -archivePath Feather.xcarchive \
            -destination generic/platform=iOS \
            -sdk iphoneos \
            -skipPackagePluginValidation \
            -quiet \
            COMPILER_INDEX_STORE_ENABLE=NO \
            ENABLE_PREVIEWS=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGN_IDENTITY=""

      - name: Export IPA
        run: |
          mkdir -p build/Payload
          cp -R Feather.xcarchive/Products/Applications/Feather.app build/Payload/
          cd build
          zip -qy -r XSign-Pro.ipa Payload
          
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: XSign-Pro-IPA
          path: build/XSign-Pro.ipa
